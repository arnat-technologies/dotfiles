# --------------------------------------------------
# LXD
#--------------------------------------------------
if [[ "$(is_installed lxc)" != "0" ]]; then
#
fi

# --------------------------------------------------
# SQL SERVER
#--------------------------------------------------
if [[ "$(is_installed sqlcmd)" != "0" ]]; then
#
fi

# --------------------------------------------------
# PACMAN
#--------------------------------------------------

if [[ "$(is_installed pacman)" != "0" ]]; then
#
fi


# --------------------------------------------------
# SYSTEMD
#--------------------------------------------------

if [[ "$(is_installed systemctl)" != "0" ]]; then
#
fi


# --------------------------------------------------
# RDP
#--------------------------------------------------

if [
if [[ "$(is_installed xfreerdp)" != "0" ]]; then
#
fi



# --------------------------------------------------
# UTILS
#--------------------------------------------------

alias pubkey="xclip -sel clip ~/.ssh/id_rsa.pub | printf '=> Public key copied to pasteboard.\n'"

alias htop='sudo htop'

alias genpass="gpg --gen-random --armor 1 13"
